/**
 * The function exports a React component that renders a heading for a React quiz.
 * @returns a JSX element, specifically an `<h2>` element with the text "React Quiz".
 */

function QuestionHead() {
  return <h2>React Quiz</h2>;
}

export default QuestionHead
