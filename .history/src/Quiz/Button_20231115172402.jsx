/* eslint-disable react/prop-types */

function Button({handleReset, handleStart, children}) {
  return (
    <>
      <button onClick={handleSthandleReset}>{children}</button>
    </>
  );
}

export default Button
